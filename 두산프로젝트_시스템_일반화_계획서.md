# 두산프로젝트1.0 Phase 0: 시스템 일반화 계획서

## 📋 Phase 0 개요

### **목표**
효성중공업 프로젝트에서 검증된 시스템을 두산중공업 및 향후 다양한 기업에 적용할 수 있도록 범용적이고 확장 가능한 형태로 일반화

### **예상 소요 시간**
0.5일 (4시간)

### **담당**
- 서대리 (Lead Developer): 시스템 일반화 및 DB 구조 개편
- 노팀장 (Technical Advisor): 기술적 검토 및 아키텍처 설계

## 🏗️ DB 구조 일반화 계획

### **1. 중앙 '회사 정보 DB' 신설**
```
📊 새로운 DB 구조:
- DB명: "회사 정보 마스터"
- 목적: 조사 대상 모든 기업의 기본 정보 관리
- 주요 속성:
  * 회사명 (Title)
  * 기업 규모 (Select: 대기업/중견기업/중소기업)
  * 주요 사업 분야 (Multi-select)
  * 설립 연도 (Number)
  * 매출 규모 (Number)
  * 직원 수 (Number)
  * 본사 위치 (Text)
  * 주요 경영진 (Rich Text)
  * 최근 주요 이슈 (Rich Text)
  * 조사 상태 (Select: 진행중/완료/대기)
  * 조사 일자 (Date)
```

### **2. 기존 6개 DB 일반화**
```
📊 일반화된 DB 구조:

1. 기업 위험 프로파일 DB (기존: 효성중공업 위험 프로파일)
   - 회사명 (Relation to 회사 정보 DB)
   - 위험 유형 (Select)
   - 위험 수준 (Select: 낮음/보통/높음)
   - 위험 설명 (Rich Text)
   - 대응 방안 (Rich Text)

2. 글로벌 경쟁 분석 DB (기존: 효성중공업 글로벌 경쟁)
   - 회사명 (Relation to 회사 정보 DB)
   - 경쟁 영역 (Select)
   - 주요 경쟁사 (Multi-select)
   - 시장 점유율 (Number)
   - 경쟁 우위 (Rich Text)
   - 위협 요소 (Rich Text)

3. 재무 프로젝트 DB (기존: 효성중공업 재무 프로젝트)
   - 회사명 (Relation to 회사 정보 DB)
   - 프로젝트명 (Title)
   - 프로젝트 유형 (Select)
   - 투자 규모 (Number)
   - 예상 수익률 (Number)
   - 리스크 요소 (Rich Text)
   - 진행 상태 (Select)

4. 태스크 관리 DB (기존: 효성중공업 태스크)
   - 회사명 (Relation to 회사 정보 DB)
   - 작업명 (Title)
   - 작업 유형 (Select)
   - 담당자 (Select)
   - 우선순위 (Select)
   - 진행 상태 (Select)
   - 마감일 (Date)

5. 코드 관리 DB (기존: 효성중공업 코드 관리)
   - 회사명 (Relation to 회사 정보 DB)
   - 코드명 (Title)
   - 코드 유형 (Select)
   - 버전 (Text)
   - 설명 (Rich Text)
   - 상태 (Select)

6. 뉴스 정보 DB (기존: 효성중공업 뉴스)
   - 회사명 (Relation to 회사 정보 DB)
   - 뉴스 제목 (Title)
   - 출처 (Select)
   - 발행일 (Date)
   - 분야 (Multi-select)
   - 요약 (Rich Text)
   - 링크 (URL)
```

## 🔧 기술적 구현 계획

### **1. 데이터 마이그레이션 스크립트**
```python
# migration_script.py
def migrate_hyosung_data():
    """효성중공업 데이터를 일반화된 DB로 마이그레이션"""
    # 1. 회사 정보 DB에 효성중공업 정보 추가
    # 2. 기존 6개 DB의 데이터를 일반화된 DB로 이동
    # 3. 회사명 관계 속성 설정
    # 4. 데이터 검증 및 확인
```

### **2. DB 생성 스크립트**
```python
# create_generalized_dbs.py
def create_generalized_databases():
    """일반화된 7개 DB 생성"""
    # 1. 회사 정보 마스터 DB 생성
    # 2. 6개 일반화된 DB 생성
    # 3. 관계 속성 설정
    # 4. 템플릿 데이터 입력
```

### **3. 데이터 입력 스크립트**
```python
# generalized_data_input.py
def input_company_data(company_name, data_dict):
    """회사별 데이터 입력 함수"""
    # 1. 회사 정보 DB에 기본 정보 입력
    # 2. 각 일반화된 DB에 회사별 데이터 입력
    # 3. 회사명 관계 속성 자동 설정
    # 4. 데이터 검증
```

## 📊 예상 효과

### **1. 즉시 효과**
```
✅ 두산중공업 프로젝트 즉시 적용 가능
✅ 효성중공업 데이터 보존 및 활용
✅ 향후 기업별 프로젝트 확장 용이
```

### **2. 장기적 효과**
```
🚀 무한한 기업 대상 확장 가능
🚀 체계적인 지식 자산 관리
🚀 재사용 가능한 템플릿 시스템
🚀 효율적인 데이터 분석 파이프라인
```

## ⚠️ 주의사항 및 리스크

### **1. 데이터 마이그레이션 리스크**
```
⚠️ 주의사항:
- 기존 효성중공업 데이터 손실 방지
- 관계 속성 설정 오류 방지
- 데이터 무결성 검증 필수
```

### **2. 시스템 호환성**
```
⚠️ 확인사항:
- 기존 스크립트와의 호환성
- API 연동 스크립트 수정 필요
- 환경 변수 업데이트
```

## 🎯 성공 기준

### **1. 기술적 성공 기준**
```
✅ 7개 일반화된 DB 생성 완료
✅ 효성중공업 데이터 마이그레이션 완료
✅ 회사명 관계 속성 정상 작동
✅ 기존 스크립트 호환성 확인
```

### **2. 비즈니스 성공 기준**
```
✅ 두산중공업 데이터 입력 가능
✅ 향후 기업 추가 용이성 확인
✅ 데이터 검색 및 분석 기능 정상
```

## 📋 다음 단계

### **Phase 0 완료 후**
```
1. Phase 1: 두산중공업 기초 정보 확보
2. Phase 2: 일반화된 DB에 데이터 입력
3. Phase 3: LLM 기반 심층 분석
4. Phase 4: 영업 전략 구체화
5. Phase 5: 최종 보고서 작성
```

---

**이 계획서를 바탕으로 Phase 0 시스템 일반화를 진행하겠습니다!** 🚀 